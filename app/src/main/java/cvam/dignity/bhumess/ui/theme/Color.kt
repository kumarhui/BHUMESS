package cvam.dignity.bhumess.ui.theme

import androidx.compose.ui.graphics.Color

// ============================================================
// BHU Ji — Modern Unified Color System
// ============================================================

// Primary — Indigo
val Primary = Color(0xFF4F46E5)
val PrimaryDark = Color(0xFF3730A3)
val OnPrimary = Color.White

// Secondary — Violet
val Secondary = Color(0xFF7C3AED)
val SecondaryDark = Color(0xFF5B21B6)
val OnSecondary = Color.White

// Tertiary — Teal
val Tertiary = Color(0xFF0F766E)
val OnTertiary = Color.White

// Background / Surface
val Background = Color(0xFFF8FAFC)
val Surface = Color.White
val SurfaceVariant = Color(0xFFF1F5F9)

// Text
val TextPrimary = Color(0xFF0F172A)
val TextSecondary = Color(0xFF475569)
val TextMuted = Color(0xFF64748B)

// Borders
val Outline = Color(0xFFCBD5E1)
val OutlineVariant = Color(0xFFE2E8F0)

// Semantic colors
val Success = Color(0xFF059669)
val Warning = Color(0xFFD97706)
val Error = Color(0xFFDC2626)
val Info = Color(0xFF2563EB)

// Resource colors
val FolderColor = Color(0xFF4F46E5)
val DocumentColor = Color(0xFF2563EB)

// PDF viewer
val PdfViewerBackground = Color(0xFF0F172A)

// External brand
val WhatsAppColor = Color(0xFF25D366)

// ------------------------------------------------------------
// Temporary compatibility aliases
// ------------------------------------------------------------
// These prevent older screens from breaking while we migrate
// them to the new semantic names.

val Pink = Secondary
val Saffron = Primary